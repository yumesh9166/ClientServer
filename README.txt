1. Copy the 'project' dir at your build server
2. Change directory to 'project'
3. Run the command "g++ -o main -std=c++11 -I inclues/ server.cpp client.cpp msgServer.cpp encodedServer.cpp main.cpp" to build the code
4. Exe created with name "main"
5. Run the Exe "./main"
